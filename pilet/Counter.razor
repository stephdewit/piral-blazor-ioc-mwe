@page "/counter"

@inject IClockService clockService

<div>
    <p>Current count: @counter</p>
    <p>
        <button @onclick="Increment">Increment</button>
    </p>
    <p>@clockService.Now()</p>
</div>

@code {
    int counter = 0;

    void Increment()
    {
        counter++;
    }
}
